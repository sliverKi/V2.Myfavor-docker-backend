databases:
  - name: v2.1-myfavor-back
    databaseName: v2.1-myfavor-back
    user: v2.1-myfavor-back
    region: singapore

services:
  - type: web
    name: v2.1-myfavor-back
    env: python
    region: singapore
    buildCommand: "./build.sh"
    startCommand: "gunicorn config.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: v2.1-myfavor-back
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4